# Environment Configuration for Swagger API Documentation
# Copy this to .env and update with your actual values

# Server Configuration
PORT=3002
NODE_ENV=development
API_BASE_URL=http://localhost:3002

# CORS Configuration - IMPORTANT for Swagger UI Access
# Multiple origins can be separated by commas
CORS_ORIGINS=http://localhost:3002,http://localhost:5173,http://localhost:3000
# Alternative: use FRONTEND_URL for a single frontend origin
# FRONTEND_URL=http://localhost:5173

# Optional: Allow all Vercel preview domains (for deployment)
CORS_ALLOW_VERCEL_WILDCARD=false

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Server Timeout (for long-running operations like web crawling)
SERVER_REQUEST_TIMEOUT_MS=1200000

# AWS Configuration (Required for Bedrock services)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Bedrock Configuration
BEDROCK_KNOWLEDGE_BASE_ID=your_knowledge_base_id_here
BEDROCK_AGENT_ID=your_agent_id_here
BEDROCK_AGENT_ALIAS_ID=your_agent_alias_id_here

# S3 Configuration (for file storage)
S3_BUCKET_NAME=your_bucket_name_here
S3_BUCKET_REGION=us-east-1

# Optional: Custom model configurations
DEFAULT_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0

# Logging Configuration
LOG_LEVEL=info

# Swagger Documentation
SWAGGER_ENABLED=true

# CORS Configuration Notes:
# - Always include your server's own URL (http://localhost:PORT) in CORS_ORIGINS
# - Include your frontend URL(s) for proper API access
# - For production, replace localhost with your actual domain names